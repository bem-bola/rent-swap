<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-BmbxuPwQa2lc/FVzBcNJ7UAyJxM6wuqIj61tLrc4wSX0szH/Ev+nYRRuWlolflfl" crossorigin="anonymous">
    <title>Document</title>
</head>
<body>
{% if message == '' or message == null %}
    <p>Bonjour {{ receiver.username|capitalize }}, </p>
    <p>{{ content|raw }}</p>
    {% if status == 'validated' %}
        <p>Vous pouvez la consulter ici : <a href="{{ absolute_url(path('app_device_show', {slug: device.slug})) }}">{{ device.title }}</a></p>
        {% elseif status == 'rejected' %}
        <p>Nous vous invitons à corriger votre annonce, puis à la soumettre de nouveau</p>
    {% endif %}
    <p>Cordialement. </p>

{% else %}
    <div>{{ content|raw }}</div>
{% endif %}

<p>L’équipe <a href="{{ absolute_url(path('app_home')) }}" class="fw-bold">Reusiix</a></p>


</body>
</html>