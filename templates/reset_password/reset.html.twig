{% extends 'base_user.html.twig' %}

{% block title %}Reusiix - Réinitialiser mot de passe{% endblock %}

{% block body %}
    <h1 class="h3 fw-700 text-center pb-3">Réinitialiser le mot de passe</h1>
    {% include '_partial/alert_user.html.twig' %}
    {{ form_start(resetForm) }}

    <div class="sub-section">
        {{ form_label(resetForm.plainPassword.first, '', {
            'label_attr' : {
                'class': 'mb-2 fw-500'
            }
        }) }}
        <span class="text-danger">*</span>
        <div class="col-12 position-relative content-password">
            {{ form_widget(resetForm.plainPassword.first, {
                'attr': {
                    'class': 'form-control mb-4 fw-300 py-3 px-3 rounded-4 password',
                    'data-type-pattern': "password"
                }
            }) }}
            <span class="rounded-4 cursor-pointer position-absolute bottom-28 end-2 toggle-password" id=""><i class="bi bi-eye fs-4"></i></span>
        </div>

        <div class="content-error bg-theme-gray bg-opacity-25 my-3">
            <p class="text-danger mb-0 font-size-08 mt-1 fst-italic input-error">{{ form_errors(resetForm.plainPassword.first) }}</p>
            <p class="text-danger mb-0 font-size-08 mt-1 fst-italic input-error-pattern d-none">
                Votre mot de passe doit contenir au moins 12 caractères, une majuscule, une minuscule et un caractère spécial.</p>
        </div>

    </div>

    <div class="sub-section">

        {{ form_label(resetForm.plainPassword.second, '', {
            'label_attr' : {
                'class': 'mb-2 fw-500'
            }
        }) }}
        <span class="text-danger">*</span>
        <div class="col-12 position-relative content-password">
            {{ form_widget(resetForm.plainPassword.second, {
                'attr': {
                    'class': 'form-control mb-4 fw-300 py-3 px-3 rounded-4 password-repeat',
                    'data-type-pattern': "password"
                }
            }) }}
            <span class="rounded-4 cursor-pointer position-absolute bottom-28 end-2 toggle-password" id=""><i class="bi bi-eye fs-4"></i></span>
        </div>

        <div class="content-error bg-theme-gray bg-opacity-25 my-3">
            <p class="text-danger mb-0 font-size-08 mt-1 fst-italic input-error">{{ form_errors(resetForm.plainPassword.second) }}</p>
            <p class="text-danger mb-0 font-size-08 mt-1 fst-italic input-error-pattern d-none">Les mots de passes doivent correspondre</p>
        </div>
    </div>

    <div class="d-flex justify-content-center">
        <button class="bg-theme-dark text-white border-0 px-5 py-3 rounded-pill" type="submit">Envoyer</button>
    </div>

    {{ form_end(resetForm) }}
{% endblock %}
