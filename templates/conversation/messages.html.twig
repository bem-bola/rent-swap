{% set userCurrent = app.user %}
<!-- entente content messages-->
{% for messagessss in messages %}
    <li class="messagessss col-8 {{ userCurrent == messagessss.author ? 'shadow-lg ms-auto': 'd-inline-block bg-primary text-white' }} px-3 py-3 rounded-4 mb-3">
        <div class="font-size-09">{{ messagessss.content }}</div>
        <div class="font-size-07 text-end">Le 10 Jan 2024 à 10:30</div>
    </li>
{% endfor %}




